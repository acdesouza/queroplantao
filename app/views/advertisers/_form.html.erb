<% if @advertiser.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@advertiser.errors.count, "error") %> impedem a criaçao da conta:</h2>

    <ul>
      <% @advertiser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@advertiser) do |f| %>
  <fieldset>
    <div class="field control-group">
      <%= f.label :email %>
      <%= f.email_field :email %>
    </div>
    <div class="field control-group">
      <%= f.label :password %>
      <%= f.password_field :password %>
    </div>
    <div class="field control-group">
      <%= f.label :password_confirmation %>
      <%= f.password_field :password_confirmation %>
    </div>
    <div class="actions control-group">
      <%= f.submit 'Criar conta', class: 'btn btn-success' %>
    </div>
  </fieldset>
<% end %>
